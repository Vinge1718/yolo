---
# tasks file for build-client
- name: Build client docker image
  docker_image: 
     name: client-image
     source: build 
     build:
        path: "{{ dockerfile_path}}"
        pull: yes

  register: image

- name: Check image
  debug:
    var : image
