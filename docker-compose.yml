version: '3.7'
services:
  client:
    image: kipkoechdon/client-image:v1.0.0
    stdin_open: true
    tty: true
    ports:
      - "3000:3000"
    volumes:
      - ./client:/app

    networks:
      - my-network
    depends_on:
      - backend

  backend:
    image: kipkoechdon/yolo-backend-image:v1.0.0
    ports:
      - "5000:5000"
    volumes:
      - ./backend:/app

    environment:
      - mongodb_url=mongodb://127.0.0.1:27017

    networks:
      - my-network
    depends_on:
      - db

  db:
    image: mongo
    ports:
      - "27017:27017"
    volumes:
      - ./data:/data/db
    networks:
      - my-network


volumes:
    data:

networks:
    my-network:
       driver: bridge

